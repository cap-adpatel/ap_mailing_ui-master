import {FilesCountPipe} from './files-count.pipe';

describe('FilesCountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilesCountPipe();
    expect(pipe).toBeTruthy();
  });
});
