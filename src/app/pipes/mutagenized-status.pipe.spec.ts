import {MutagenizedStatusPipe} from './mutagenized-status.pipe';

describe('MutagenizedStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new MutagenizedStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
