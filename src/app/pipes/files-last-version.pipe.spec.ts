import {FilesLastVersionPipe} from './files-last-version.pipe';

describe('FilesLastVersionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilesLastVersionPipe();
    expect(pipe).toBeTruthy();
  });
});
