import {FilesLastUpdatePipe} from './files-last-update.pipe';

describe('FilesLastUpdatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilesLastUpdatePipe();
    expect(pipe).toBeTruthy();
  });
});
