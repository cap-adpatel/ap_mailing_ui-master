import {PreanalyticInfoPipe} from './preanalytic-info.pipe';

describe('PreanalyticInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new PreanalyticInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
