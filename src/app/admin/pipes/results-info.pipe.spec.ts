import {ResultsInfoPipe} from './results-info.pipe';

describe('ResultsInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new ResultsInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
